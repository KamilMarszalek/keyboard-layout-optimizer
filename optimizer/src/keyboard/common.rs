/// Number of characters in the ASCII table.
pub const ASCII_COUNT: usize = 128;

/// A single ASCII character represented as a byte.
pub type AsciiChar = u8;

/// Index of a key within a keyboard layout or geometry array.
pub type KeyIndex = usize;
