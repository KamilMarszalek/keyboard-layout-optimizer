pub const US_KEY_COUNT: usize = 26 + 10 + 11; // letters + digits + punctuation
pub const US_PRESS_COUNT: usize = 2 * US_KEY_COUNT;

pub const ASCII_COUNT: usize = 128;
pub type AsciiChar = u8;
pub type KeyIndex = usize;
